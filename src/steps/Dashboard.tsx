export default function Dashboard() { // Route component displayed only after the guard authorizes access.
  return <p>🎉 Welcome to your dashboard — this is protected content.</p>; // Returns this route's UI as JSX.
}
